<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.studist.rinon2238.imagescalingteststable">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImageScalingTestStable">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PhotoViewDemoActivity"
            android:exported="false"/>

        <activity android:name=".ZoomageViewDemoActivity"
            android:exported="false"/>

        <activity android:name=".DIYReferenceOfficialActivity"
            android:exported="false" />
    </application>

</manifest>